const EApplicationEnvironment = {
    PRODUCTION: 'production',
    DEVELOPMENT: 'development'
};

const USER_ROLES = {
    USER: 'user',
    ADMIN: 'admin'
};

module.exports = { EApplicationEnvironment, USER_ROLES };
